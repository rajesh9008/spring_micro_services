spring:
  application:
    name: zuul-api-gateway
  cloud:
    config:
      uri: http://localhost:8088

---

#spring:
#  profiles: docker
#  cloud:
#     config:
#        uri: http://configserver:8888